# 引き継ぎ — 新しいセッションは作業前にこれを読む

このファイルはリポジトリ内にあります。clone した時点で必ず手に入るので、
claude.ai プロジェクトに紐づいていないセッションでも読めます。

---

## 1. これは何のリポジトリか

**codeschoolnavi.com** — 日本のプログラミングスクール比較アフィリエイトサイト。
目的は「ほぼ完全放置で収益が出る仕組み」をつくること。サイトを作ること自体は目的ではない。

- 本番: https://codeschoolnavi.com （GitHub Pages、HTTPS 強制済み、独自ドメインはムームードメイン）
- 記事の自動生成・公開は GitHub Actions の cron `0 2 * * *` UTC（= 11:00 JST）。
  **セッションを開いていなくても毎日動く。** 会話が途切れてもサイトは止まらない。
  ただし **実際に起動するのは 05:30〜06:00 UTC（14:30〜15:00 JST）ごろ**。
  GitHub のスケジュール実行は混雑時に数時間ずれる（実測: 07-26 は 05:36Z、07-27 は 05:55Z）。
  **11:00 JST に走っていないからといって壊れたと判断しない。** 夕方まで待ってから調べる。
- 予算はひと月あたり 0〜10,000円。オーナーは手を動かさない。
  例外は「私（Claude）が規約上できないこと」だけ = アカウント作成・ログイン・支払い・
  APIキーの入力・本人のメールボックス内のリンクを開くこと。

## 1.5 現在地（2026-07-28 時点）

| 項目 | 状態 |
|---|---|
| 公開記事 | **11本**（2026-07-28 にキカガクが自動公開。cronは正常に回っている） |
| Google Search Console | **登録済み**（ドメインプロパティ／DNS の TXT で自動確認） |
| サイトマップ | 送信済み・ステータス「成功しました」 |
| インデックス | `/` と `/blog/hyoban-dmmwebcamp/` は登録済み。残りは「検出 - インデックス未登録」で、うち8ページは手動リクエスト済み。**samurai・aidemy の2本は公開が遅れたぶんリクエストがまだ**（次にやる） |
| もしもアフィリエイト | 提携中2件（SHIFT TERAS CAMPUS・楽天）／申請中15件／**否認1件（ウズウズIT・新規会員登録12,000円）** |
| A8 | **2026-07-28 に立て直した。** 旧「5件申請中」は事実無根だった（§12）。同日サイト「コードスクールナビ」を登録し5件を申請し直し、**SkillHacks は即時提携で提携完了**、残り4件は審査中 |
| 収益 | ゼロ。ただし **2026-07-28 に収益動線を実装し、サイト上に初めてクリック可能なアフィリエイトリンクが載った**（§11） |

**サイトはまだ5日目**（リポジトリ初コミット 2026-07-24）。期限は初成約 2026-10-21、月5万円 2027-01-19、月20万円 2027-07-23。

> **2026-07-27 に見つけた重大な不具合（修正済み）**
> cron は 07-26・07-27 の 2 回とも success で記事を書いてコミットしていたが、
> **`deploy-pages` が一度も起動せず、本番は 8 本のまま止まっていた。**
> `daily-content-cycle` は既定の `GITHUB_TOKEN` で push するが、GitHub は再帰を防ぐため
> このトークンによる push では workflow を起動しない。`pages.yml` のトリガーが
> `push` on `site/**` だけだったため、記事が増えても永久に発火しなかった。
> Netlify はリポジトリを直接監視していたので起きず、**GitHub Pages への移行時に埋まった**。
> `pages.yml` に `workflow_run` トリガーを足して修正済み（`48fafcf`）。

## 2. 絶対に守る 3 点

1. **`git push` はできない。** 認証情報が無く `fatal: could not read Username for 'https://github.com'`
   になる。`gh` コマンドも無い。ただし **`git fetch origin` は動く**（公開リポジトリの読み取りは
   認証不要）。コミット後に `git fetch origin && git reset --mixed origin/main` でローカル HEAD を
   合わせれば、`git status` に本当に残っている差分だけが出る。
2. **コミットは GitHub の「Upload files」UI 経由で行う。**（手順は §4）
   ブラウザのエディタ（CodeMirror）に長い文字列を打ち込む方式は、およそ 6,000 字に 1 文字の割合で
   静かに壊れるので使わない。Upload files はバイト列をそのまま送るので壊れない。
3. **本番を目で見る工程を省略しない。** 「Actions が success」は「本番に出ている」の
   証明にならない（§1.5 の不具合が実例）。**必ず本番の URL を叩いて 200 を確認する。** 文法チェックも表記チェックも通り抜ける日本語崩れが
   実際に起きた（例: 「専門実践は受講中50%に修了後の条件を満たすと最大80%です」— 機械的な
   言い換えの副作用で助詞が壊れた）。最後に必ず本番のスクリーンショットを目で読む。
   機械側の backstop としてこの grep を回す:
   ```bash
   grep -nE '(をを|がが|はは|にに|でで|とと|、、|。。|[ぁ-んァ-ヶ一-龥]、と|%に修了|ますが、が)' *.md
   ```

## 3. セットアップ

```bash
git clone https://github.com/yurunebals-ux/codeschoolnavi /tmp/csn
cd /tmp/csn && npm install && cd site && npm install
```

- サイトは Astro 4.16（`site/`）、記事生成パイプラインは TypeScript + tsx（`src/`）。
- 生成は gpt-4.1-mini（OpenAI API）。`LLM_API_KEY` は GitHub Secrets にあり、
  サンドボックス内には無い。だからローカルでは常にオフライン扱いになる（`isOffline()` が true）。
- `npx tsc --noEmit -p .` は `TS2688 Cannot find type definition file for 'node'` で失敗する。
  これは以前から存在する既知の問題で、ビルドとデプロイには影響しない。
- Astro のテンプレート内で `<=` をそのまま書くとコンパイルエラーになる。

## 4. コミット手順（Upload files UI）

1. 変えたファイルの `git hash-object <file>` を取る。
2. リポジトリ側の path→sha を 1 回の API 呼び出しで取る:
   `https://api.github.com/repos/yurunebals-ux/codeschoolnavi/git/trees/main?recursive=1`
   → sha が違うファイルだけアップロードする。
3. アップロード用のファイルは **`/mnt/user-data/working/up/`** にディレクトリ構造を再現して置く。
   `file_upload` は `/tmp` 配下を受け付けない。1 回の合計は 10MB 未満。
4. アップロード input は `webkitdirectory:false` なので、まとめて上げると全部リポジトリ直下に
   落ちてしまう。**ディレクトリごとに** `https://github.com/yurunebals-ux/codeschoolnavi/upload/main/<dir>`
   を開いてから上げる。
5. `find` で "choose your files file input" を探して ref を取り、`file_upload({paths, ref, tabId})`。
6. コミット欄（エディタのダイアログとは別物）:
   `#commit-summary-input` / `#commit-description-textarea` /
   `input[name="commit-choice"][value="direct"]`（既定でオン）/ ボタンの文字は正確に `Commit changes`。
7. tree API を再取得して全 blob sha を照合する。
8. `git fetch origin && git reset --mixed origin/main`。
   ただし `data/pricewatch.json` と `site/src/data/pricewatch.json` は CI が毎日更新していて
   リモートのほうが新しいので、`git checkout --` でリモート側を採用する（上書きすると価格監視の
   履歴が巻き戻る）。

## 5. 検証のときの注意

- 確認は Contents API / tree API を使う。**raw.githubusercontent.com は古いキャッシュを返す。**
- 本番ページの fetch は **codeschoolnavi.com のタブから** 実行する（github.com のタブからだと
  CSP で外部 fetch が止まる）。URL に `?cb=` + 乱数を付け、`cache:'no-store'` を指定する。
- `deploy-pages` の run が `cancelled` になるのは `concurrency: pages` の正常動作。
  `site/**` を触るコミットを連続で入れると前の run が畳まれる。**最後の run が success なら問題ない。**
- `src/`・`data/`・リポジトリ直下だけを触るコミットはデプロイを起こさない。
- Actions の状態は `…/actions/runs?per_page=N` で未認証でも読める。`…/pages` は未認証だと 404。
- javascript_tool は URL や長い base64 を返そうとするとブロックされる。真偽値・件数・長さ・
  短いハッシュだけを返す。`Runtime.evaluate` は 45 秒で強制終了。ページ遷移をまたぐ値は
  `window.__X` ではなく `sessionStorage` に置く。
- **`GITHUB_TOKEN` による push は workflow を起動しない**（GitHub の再帰防止仕様）。
  CI が自分でコミットして、それを別の workflow で拾う設計にするときは、
  `push` トリガーではなく **`workflow_run`** を使う。PAT は不要。
  `types: [completed]` は失敗回でも発火するので、`if: github.event.workflow_run.conclusion == 'success'`
  で絞らないと、記事が書けなかった日に古い内容でデプロイしてしまう。
- **両方の workflow が success を返すのに成果物だけ無い、という壊れ方がある。**
  ログを見ても異常に見えないので、**公開されているはずの URL を実際に叩いて確認する**。
  記事を1本足したら `…/blog/<slug>/` が 200 か、`sitemap-0.xml` の `<loc>` 件数が増えたかを見る。
- **Astro のスコープ付き `<style>` は、JS が実行時に作った要素には当たらない。**
  スコープ付きの規則は `.foo[data-astro-cid-xxx]` に変換されるが、`innerHTML` で生成した要素には
  その属性が付かないため、CSS が丸ごと無効になる。実際に給付金の判定パネルで踏んだ。
  実行時に作る要素の規則だけ `<style is:global>` に出し、**すべて `#id` 配下に限定**して漏れを防ぐ。
- **ネイティブの `confirm()` ダイアログはページを固める。** ムームードメインの「設定を追加」が該当。
  出ている間は `Runtime.evaluate` が 45 秒でタイムアウトし、拡張のスクリーンショットも失敗する。
  Chrome は read ティアなので Claude 側からは押せない。**オーナーに OK を押してもらう**しかない。
  固まったら壊れたと判断せず、まず画面を見る（`mcp__computer-use__screenshot` は撮れる）。
- **GSC の URL 検査ボックスは、ページ遷移と同じバッチ内でクリックしても入力が入らない。**
  `navigate` を含むバッチの後は、**別バッチで** クリック→3〜4秒待つ→入力、の順にする。
  それでも 2 回に 1 回は空振りするので、スクリーンショットで入ったことを確認してから Enter。

## 6. 記事の事実関係（教育訓練給付金）

- 一般教育訓練 20%（上限 10万円）
- 特定一般教育訓練 40%（上限 20万円）
- 専門実践教育訓練 受講中 50%（年間上限 40万円）＋ 修了後の就職等 20% ＋ 賃上げ 10%
  = **最大 80%（年間上限 64万円）**
- 雇用保険の加入期間は一般・特定一般が 1年以上、専門実践が 2年以上
- 専門実践・特定一般は **受講開始日の 2週間前まで** にハローワークで手続き

「最大 70%」は古い数字。記事に出てきたら誤りなので直す。

## 7. E-E-A-T の線引き（オーナーが明示した方針）

編集長は **「佐倉 透（さくら とおる）」**。これはペンネームであり、ページ上でその旨を明示している。

- OK（演出）: ペンネーム、イラストのアバター、一人称の語り、編集部としての見解
- NG（捏造）: 実在しない経歴・保有資格・受講体験・利用者レビュー
  （景品表示法／ステマ規制に触れる）

デメリットと向かない人を必ず書く、というのがサイトの唯一のルール。

## 8. 詳しい版はどこにあるか

claude.ai プロジェクト **「アフィリエイト　海外」** の `claude/進捗と次のステップ.md`。
`project_read` が使えるセッション（= そのプロジェクトに紐づいたセッション）ならそちらを読む。
使えないセッションでは、このファイルが唯一の引き継ぎ資料になる。

## 9. 次にやること

**オーナーにしかできないもの（ここが詰まると先に進まない）**

1. **info@codeschoolnavi.com の受信設定** — ムームーのメール転送。サイトに訂正窓口として
   記載済みなので、届かない状態を長く放置しない。
2. **Netlify を無料プランへ落とす** — $9/月の停止。GitHub Pages 経路が安定して動いているので、
   もう課金する理由はない。ロールバック用にサイト自体は残す。
3. **もしも／A8 のログイン** — 提携状況を見るたびに必要。Claude はパスワードを入力できない。

**Claude が進められるもの**

4. **もしもの提携承認を定期的に確認する** — `apply_status=1` が申請中、`2` が提携中、`3` が否認。
   `…/af/shop/promotion/search?apply_status=2&limit=100` で一覧できる。承認が出たら
   広告リンクを取り、`data/affiliates.json` の該当 tool を差し替える（手順は §4 と下記）。
   **A8 は 2026-07-28 に申請し直した（§12）。忍者CODE/ポテパン/DMM生成AI/Winスクールの4件が審査中。**
5. ~~記事内に収益動線がない~~ → **2026-07-28 実装済み。§11 を読むこと。**
   承認が出たら `data/affiliates.json` の `affiliate_url` を差し替えるだけで全ページが切り替わる。
5b. **ポテパンキャンプの料金が実態と食い違っている可能性。** 公式 `camp.potepan.com` のタイトルが
   「**無料**プログラミングスクール｜29歳以下・Web開発企業へ転職｜ポテパンキャンプ**U29**」になっている。
   記事は「44万円台」で書いてある。無料のU29コースが別立てなのか、料金体系ごと変わったのかは未確認。
   **料金の誤りは景表法上まずいので、次のセッションで公式を読んで確かめる。**
6. **記事ページは本文が始まるまで約 1,075px ある。** アイキャッチ→目次→料金監視→広告表記→
   （記事によっては）名称変更、と箱が積み上がっている。読み始めるまでが遠い。
7. **「払った額に対する見返り」が 6 記事に 7 箇所ある。** 「費用対効果」を機械置換した副作用で、
   「事前に確認し払った額に対する見返りを計算してください」のように読みづらい。まとめて直す。
8. 記事が 15 本を超えたらカテゴリタブを追加。
9. Phase 2: 英語版。

### もしもの広告リンクの取り方（確立済み）

1. `…/af/shop/promotion/search?apply_status=2&limit=100` で提携中の一覧を出す
2. 各行の「広告リンクへ」の `href` から `promotion_id` を取る
3. `…/af/shop/promotion/source?promotion_id=<id>&shop_site_id=682146&display_type=1`
   （`display_type=1` がテキスト広告。省略するとバナー）
4. ソースの textarea から `a_id`／`p_id`／`pc_id`／`pl_id` を取り出す。
   **URL をそのまま返そうとすると javascript_tool にブロックされる**ので、数値だけ取る
5. `https://af.moshimo.com/af/c/click?a_id=…&p_id=…&pc_id=…&pl_id=…` に組み立てる
   （原稿側の href はスキーム無しの `//af.moshimo.com/…`。`https:` を足すこと）
6. 組み立てた文字列とソースの実値を **`v.includes(...)` で照合してから** 採用する

SHIFT TERAS CAMPUS（旧 DMM WEBCAMP）は `promotion_id=1363`、成果は
無料カウンセリング申込 1,000円（ほぼ全承認）／参加完了 15,000円（月次手動）／本申込 25,000円。
本人申込 NG・広告出稿 NG・再訪問 30日。`reward_yen` には確実に出る 1,000 円を入れてある。

### GSC でこれから見るもの

- 検索パフォーマンスは登録の翌日以降にデータが出る。**表示回数が付き始めたら初回接触が起きている。**
- 「ページ」レポートで「検出 - インデックス未登録」が減っているかを見る。減らない場合は
  内部リンクが薄いか、記事が薄いと判断されている可能性がある。
- インデックス登録リクエストは 1 日あたりの上限がある。優先度の高いページから使う。

## 10. 期限（オーナーの必達目標）

起点は 2026-07-23（A8 へ一括申請した日）。

- 初成約まで 90日 → **2026-10-21**
- 月 5万円まで 180日（必達）→ **2027-01-19**
- 月 20万円まで 365日 → **2027-07-23**

## 11. 収益動線（2026-07-28 実装）

### それまで何が起きていたか

公開10本の外部リンクは、料金監視ボックスの「公式サイト」1本だけだった。しかもそれは素の公式URLで、
**サイト全体でクリックしても1円にもならない状態**が続いていた。原因は「リンクを記事本文に焼き込む」設計にある。

1. 提携が未承認のあいだ `affiliate_url` はプレースホルダなので、`quality.ts` が原稿ごとハードブロックする（＝入れたくても入らない）
2. 仮に入れられたとしても、提携が承認された日に全記事を書き直すことになる（LLMコストと品質リスク）

### どう直したか

**導線を本文からテンプレートへ移した。** `src/pipeline/offers.ts` が `data/affiliates.json` を読んで
`site/src/data/offers.json` を生成し、`site/src/components/Cta.astro` がそれを描画する。

- 提携済み → アフィリンク＋`rel="sponsored"`＋PRバッジ
- 未承認 → 公式サイトへ（`rel="nofollow"` のみ。PRは出さない。広告でないものを広告と偽らない）
- 出せるURLが無い → **CTAごと描画しない**（空振りリンクを作らない）
- プレースホルダが1つでも混ざったら `buildOffers()` が例外で落ちる

`publishRun()` が毎サイクル `syncOffers()` を回す。

> **承認が出た日にやること: `data/affiliates.json` の該当 tool の `affiliate_url` を1行差し替えるだけ。**
> 翌日のcronで全ページのCTAが自動的に収益リンクへ変わる。記事の書き直しは要らない。

### 出している場所（5箇所）

| 場所 | 位置 |
|---|---|
| 記事：料金監視ボックスの「公式サイト」 | 記事の一番上。唯一の既存外部リンクだったので、ここが一番効く |
| 記事：本文中盤 | そのスクールの「特徴・向いている人・注意点」を読み終えた直後（図解より後ろ） |
| 記事：末尾 | 読み切った人向け |
| トップ：ピックアップカード | オレンジの本命ボタン。「評判をくわしく見る」は輪郭線だけの副ボタンに落とした |
| トップ：比較表 | スクール名セルの中に「公式サイト ↗」 |

現在アフィリンクになっているのは **SHIFT TERAS CAMPUS の1件だけ**（もしも・無料カウンセリング申込1,000円）。
記事に3本・トップに2本で計5本。残り10校は公式サイトへの素のリンク。

**リンクが実際に着地することを確認済み**: `af.moshimo.com/af/c/click?...` → `lp.web-camp.io/...&utm_source=affiliate-moshimo`。

### ここで踏んだ罠（同じことを繰り返さないために）

- **`main article a[rel~="sponsored"]` が、こちらで見た目を決めたリンクまでボタン化した。**
  この規則はMarkdown由来の素のリンクをオレンジのボタンに変えるためのものだが、rel だけで判定していたため、
  料金監視ボックスの「公式サイト」を提携リンクにした瞬間そこにも当たり、
  「※価格そのものは必ず【公式サイト PR ↗】でご確認ください。」と**文の途中に大きなボタンが挟まった**。
  → `:not([class])` を足した。**これは本番のスクリーンショットを見て初めて見つかった。**
  リンク数・rel・デッドリンクの機械検査は全部通過していた。
- **比較表に「公式」列を足したら、横スクロールで画面外に出て誰にも押せなかった。**
  表は `overflow-x:auto` + `white-space:nowrap`。右端の列は存在しないのと同じ。
  → 列を増やすのをやめてスクール名セルに入れ、1列目を `position:sticky` で固定した。
- **カード内にオレンジのボタンを2つ縦に並べると、どちらが本命か分からなくなる。**
  → 内部リンク側を輪郭線だけの副ボタンにした。
- CTAの文言は場所で変える。カードや表の中はスクール名がすぐ隣にあるので、
  名前を繰り返すと3行に折れる（`label` と `short` を分けてある）。

## 12. A8：「5件申請中」は事実ではなかった（2026-07-28 判明）

これまでの引き継ぎには「A8に5件申請中（2026-07-23／ninjacode・potepan・dmmaicamp・winschool・skillhacks）」とあり、
承認待ちの収益トリガーとして扱っていた。**ログインして確認したところ、その5件は存在しない。**

| 項目 | 実際 |
|---|---|
| メディアID | `a26040297282` / **サイト名 `salary_money30`** |
| 申込中プログラム | **0件** |
| 参加中プログラム | 6件（松井証券iDeCo・楽天アフィリエイト・お名前.com・A8メディア会員募集・セルフバック×2）＝**全部デフォルトか無関係** |
| 解除／終了／新着承認 | いずれも0件 |

**オーナーに確認済み: A8のアカウントは他に無い。** つまり申請は送信されていなかった。

### 同日中に立て直した（2026-07-28）

サイト「**コードスクールナビ**」を登録（ウェブサイトID **002**／副サイト。主サイトは `salary_money30` のまま）。
カテゴリ=教育・学習・教養、開設2026/07、月間訪問者5以下、月間PV100以下、紹介文は**全角100文字以内**の制限あり。

そのうえで5件を申請し直した結果:

| プログラム | プログラムID | 結果 |
|---|---|---|
| SkillHacks / MovieHacks | `s00000021268001` | **提携完了**（即時提携） |
| 忍者CODE | `s00000025234001` | 審査中 |
| ポテパンキャンプ | `s00000017938002` | 審査中 |
| DMM 生成AI CAMP | `s00000027398001` | 審査中 |
| Winスクール | `s00000023621001` | 審査中 |

> **⚠️ A8の広告リンク（a8mat）は登録サイトごとに別物。**
> 同じ素材でも コードスクールナビ=`4B8B4S+AP7HY2+…` / salary_money30=`4B8B4S+AP7H6A+…`。
> **間違えると成果が別サイトに計上される。** 発行画面で必ず「コードスクールナビ」を選ぶこと。

SkillHacks の広告リンクは `data/affiliates.json` に反映済み（着地先 `brain-holdings.com/skillhacks/` を実際に開いて確認）。
ただし **SkillHacks はまだ記事も掲載枠も無いのでページには出ない。** 記事が生まれた時点で自動でCTAが出る。


`data/affiliates.json` は実態に合わせて修正済み（各toolの `a8_status` を「未申請」に、`_asp_note` に経緯を記録）。
`a8_applied_ids` は残してあるが、これは**「申請しようとした対象」であって「申請済み」ではない**（`a8_applied_ids_note` に明記）。

### 画面の歩き方（次回のため）

- 提携申請: `…/a8v2/media/joinPrograms/detail.do?action=confirmSearch&insIds=<プログラムID>&searchFlg=1&viewType=0`
  → `select[name=selectedWebsiteIds]` で「コードスクールナビ」を選び「提携申請をする」
- 広告リンク: `…/a8v2/media/linkAction.do?insId=<プログラムID>`
  → `select[name=websiteId]` で「コードスクールナビ」、`select[name=materialDesc]`=テキスト → 「広告リンクを表示」
  **選び直したら必ず「広告リンクを表示」を押し直す。** 押さないと前のサイトのリンクが表示されたままになる

- 参加中: `…/a8v2/media/partnerProgramListAction.do?act=search&viewPage=`
- 申込中: トップの「申込中プログラム」リンク（`…/a8v2/media/programListAction.do?type=app`）
- **「サイト情報の登録・修正」はID/パスワードの再入力を求められるのでClaudeでは開けない**
- **管理画面で href や Cookie らしき文字列を返そうとすると javascript_tool がブロックする。**
  返すのは真偽値・件数だけにする

## 13. 見た目の作り込み（2026-07-28）

オーナー指摘「デザイン的に見た目が寂しい」への対応。**トップページの `<img>` は0枚だった。**

### 写真を使わなかった理由（次に同じ相談が来たとき用）

- **作業コンテナから画像をダウンロードできない。** Unsplash / Pexels / picsum いずれも HTTP 000（外部ネットワークが塞がれている）。
  `git clone` と npm は通るが、一般のWeb取得は不可。**写真を使うならオーナーが会話に添付するしかない。**
- そもそもこの手の比較サイトに定番のストック写真を敷くと、**かえって量産アフィサイトに見える**。
- **人物写真を使ってはいけない場所がある**（E-E-A-Tの線引き）:
  - **編集長 佐倉 透に人物写真はNG。** ペンネーム＋イラストは演出だが、他人の顔写真を出すと実在の人物の捏造になる
  - **口コミ・評判の近くに「受講生っぽい人物写真」もNG。** 実際の受講生の声だと誤認させると景表法・ステマ規制のリスク
  - 使えるのは人物が主役でない装飾的な画像だけ

### やったこと（すべてSVG/CSS・追加リクエスト0）

| 場所 | 内容 |
|---|---|
| ヒーロー | `HeroArt.astro`。「横並びの比較」を絵にした（長さの違う棒が同じ起点にそろい、1本だけオレンジで手前に出る） |
| 目的から探す | `NavIcon.astro`。絵文字→単色SVG。カードごとに色を持たせた |
| 新着記事 | 最新1本を大きく（`.feat`）＋残りは横組みの小カード（`.card.row`） |
| 給付金ガイド | 先頭に `.kbanner`。3階建てと「2週間前まで」を入口に出す |

**実測: ページ全体 5,557px → 4,759px、記事一覧 1,842px → 1,037px。** トップのHTMLは38KB。

### ここで踏んだこと

- **絵文字をUIに使わない。** OSごとに字形・色・サイズが変わり、4つ並ぶと散らかる。フォントが無い環境では豆腐になる
- **横118pxのスロットに640×360のサムネを `slice` で入れると、中のタイトルが左右に切れて「壊れている」ように見えた。**
  → `Thumb.astro` に `compact` を追加し、**文字を持たない正方形タイル**を別に描く。配色とモチーフの選び方は同じ規則なので世界観はそろう
- ヒーローの装飾は最初オレンジの棒が本文の裏を横切って読みにくかった。**マスクで左62%を完全に隠し**、棒の位置も右へ寄せた
- **`html{scroll-behavior:smooth}` があるので、`scrollTo` 直後のスクリーンショットは移動途中の絵になる。**
  検証で位置を変えるときは `behavior:'instant'` を指定するか、`locator(...).screenshot()` で要素そのものを撮る
